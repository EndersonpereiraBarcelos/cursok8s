apiVersion: v1
kind: Pod
metadata:
  name: primeiro-pod-declarativo
  labels:
    name: myapp
spec:
  containers:
  - name: nginx-container
    image: nginx:lastest
    # resources:
    #   limits:
    #     memory: "128Mi"
    #     cpu: "500m"
    # ports:
    #   - containerPort: <Port>
